extends base.pug
block content
    if errorMessage
        span=errorMessage

    form(method="POST")
        input( type="radio", required, id="short", name="formType", value="1", onclick="chooseForm(this.name)", checked="checked")
        label( for="short") 단답형
        input( type="radio", required, id="multiple", name="formType", value="2", onclick="chooseForm(this.name)")
        label( for="multiple")   객관식
        br
        br
        div(id="form_1")
            label(for="Question") 질문:
            input(placeholder="Question", required, type="text",id="Question", name="Question")
            label(for="Answer") 정답:
            input(placeholder="Answer", required, type="text", id="Answer",name="Answer")
            input(type="submit", value="Upload Test")

        div(id="form_2" style="display: none;")
            label(for="Question") 질문:
            input(placeholder="Question", required, type="text",id="Question", name="Question")
            label(for="Answer") 정답:
            input(placeholder="Answer", required, type="text", id="Answer",name="Answer")
            label(for="WrongAnswer") 틀린 선택지1:
            input(placeholder="WrongAnswer", required, type="text", class="WrongAnswer",name="WrongAnswer")
            label(for="WrongAnswer") 틀린 선택지2:
            input(placeholder="WrongAnswer", required, type="text", class="WrongAnswer",name="WrongAnswer")
            label(for="WrongAnswer") 틀린 선택지3:
            input(placeholder="WrongAnswer", required, type="text", class="WrongAnswer",name="WrongAnswer")
            input(type="submit", value="Upload Test")

block script 
    script(src="/frontJS/chooseQuestionType.js")
        