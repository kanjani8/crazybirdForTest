extends base
include mixins/score
include mixins/posting

block content
    if loggedIn
        div.whole__box.see
            div.left__box.see
                a(href=`${subject.id}/community`) 게시판
                div.postingBox
                    each posting in postings
                        +posting(posting)
                    else
                        li Sorry nothing found.
                    h4(style= "color: pink;") + 글이 10개 미만입니다. +
            div.right__box.see
                a(href=`${subject.id}/test/setting`) 문제 풀기
                a(href=`${subject.id}/test/list`) 문제 리스트
                hr
                div.scoreBox
                if scores
                    each score in scores 
                        +score(score)


    else
        a(href="/login") 문제와 게시판을 보려면 먼저 로그인 하기 &rarr;