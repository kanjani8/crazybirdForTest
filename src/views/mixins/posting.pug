mixin posting(posting)
    div
        h4
            a(href=`/subject/${posting.subject}/community/${posting.id}`)=posting.title
            small=posting.createdAt
            h4 추천수: #{posting.meta.rating}
        hr