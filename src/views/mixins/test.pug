mixin test(test)
    div
        h4
            a(href=`/subject/${test.subject.id}/test/${test.id}/edit`)=test.question
            small=test.answer
            a(href=`/subject/${test.subject.id}/test/${test.id}/delete`) ❌
        hr