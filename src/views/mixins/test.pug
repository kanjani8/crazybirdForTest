mixin test(test)
    div
        h4
            a(href=`/subject/${subject.id}/test/${test.id}/edit`)=test.question
            small=test.answer
            a(href=`/subject/${subject.id}/test/${test.id}/delete`) ❌
        hr