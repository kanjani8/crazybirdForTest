mixin subject(subject)
    div
        h4
            a(href=`/subject/${subject.id}`)=subject.name
            small=subject.professor
        if subject.school
            h5=subject.school.name
        else
            h5 학교공통
        a(href=`/subject/${subject.id}/like`) ☆
        hr